# Reporting Triple Output (JSON / Excel / HTML)

A tiny Python tool that generates the same report in **JSON**, **Excel (.xlsx)** and **HTML**.

## Run
```bash
pip install -r requirements.txt
python generate_report.py
